# Interview Director P6-4｜Runtime Integration 实现与验收

> 日期：2026-09-08
> 状态：**PASS / OPT_IN_RUNTIME_INTEGRATION**

## 实现

Director 已接入正式 Runtime / Host，但仍保持 opt-in：

```text
Legacy Session
→ observe
→ next_turn

Director Session
→ observe
→ director_decide
→ next_turn / CLOSE_ROUND
```

实现边界：
- Runtime `status` 暴露 4 个 Director 操作；
- `director_prepare_session` 将单个 Session 标记为 `strategy.director.v1`；
- `next_turn` 接收 `targetSignalId / preferredFamilyId`，Retrieval Authority 不变；
- Host 自动识别 Director Plan，不要求调用方再传 `desiredAction`；
- `CLOSE_ROUND` 不再产生下一题；
- 无 Director Plan 的旧会话完全兼容。
## 验收

- P6-4 targeted：`5/5 PASS`；
- P6-3 + P6-4 Director：`17/17 PASS`；
- 兼容大 Gate：34/36 PASS，唯一两项为历史同类性能阈值抖动；
- Blast-radius 隔离复验：Voice Runtime `8/8 PASS`，Host `3/3 PASS`；
- `git diff --check = PASS`。

裁决：两个大 Gate 性能失败属于共享旧 Mac 上的 harness / scheduler jitter，不修改产品代码。

## 结论

`P6_4_RUNTIME_INTEGRATION = PASS`。

Director 已真实处于“Answer Observation 与 Runtime Retrieval 之间”的控制层，同时保留 Legacy Session 回退路径。

下一阶段：`P6-5 Interviewer Archetype + Loop Templates`。