# 面试成长系统｜P4 自适应训练实现与验收

> Status: **P4_ADAPTIVE_TRAINING_PASS**
> Date: 2026-09-08

## 结论

P4 已把 P3 的 Growth 数据变成可执行训练决策，而不是停留在复盘档案。

正式能力：

- `interview.plan_training`：根据 Review Queue、Growth Profile、OPEN Training Action、JD Focus 和历史训练状态生成下一轮计划；
- `AUTO / REVIEW / WEAKNESS / JD / MIXED` 五种训练模式；
- 只输出 `P0/P1/P2 + reasons`，内部启发式权重不冒充候选人能力分；
- Question Family 计划项必须存在 ACTIVE 正式题并通过 Route eligibility；
- `next_turn.preferredFamilyId` 把计划真正落到正式题库，同时保持全场去重；
- Topic / Learning / Resume action 不伪造成面试题；
- `interview.complete_review`：复习完成后按 NEEDS_RETRY / IMPROVED / STABLE 进入 1 / 3 / 7 天后的下一次复习。

## Gate

- P4 targeted：10/10 PASS；
- P4 + P3 Growth + Feedback + Context：31/31 PASS；
- Runtime Host isolated：3/3 PASS；
- `git diff --check`：PASS；
- 默认 `127.0.0.1:4318` 已加载 `plan_training / complete_review`，健康检查 ready=true；
- Career Facts mutation：0。

## 产品效果

训练链已从“按场景随机取下一题”升级为：

`历史成长数据 → 可解释训练计划 → 正式 Family → Runtime 提问 → 回答/复盘 → 更新成长状态 → 间隔复习`。

P4 完成后，产品主线进入 P5 Knowledge Expansion / Continuous Feedback。P5 先做 Coverage/Freshness 审计，再按真实缺口扩内容；禁止回到题量驱动。
