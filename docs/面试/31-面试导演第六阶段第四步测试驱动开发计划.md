# Interview Director P6-4｜Runtime Integration TDD

> 日期：2026-09-08
> 状态：**TDD_PLAN_V1 / OPT_IN_INTEGRATION**

## 目标

把 P6-3 Director 接到现有 Runtime/Host，不改变无 Director Plan 的旧会话。

## RED Gate

1. Runtime `status` 暴露四个 Director 操作；
2. `director_prepare_session` 通过正式 Runtime 调用并将 Session 标记 `strategy.director.v1`；
3. `next_turn` 接受 Director 给出的 `targetSignalId`，仍受 Route / dedup / closed signal 约束；
4. `/interview/continue` 对 Director Session 固定执行 `observe → decide → next`；
5. Director `CLOSE_ROUND` 时不再取新题；
6. Legacy Session 没有 Plan 时继续 `observe → next_turn`，行为兼容；
7. Host 不泄露 hidden score/reasonCode 到自然问题文本；
8. Director integration 不修改 Career Facts。

P6-4 不负责默认 Loop/Profile；那属于 P6-5。