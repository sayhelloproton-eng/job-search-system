# 语音集成与反馈闭环里程碑验收

> Date: 2026-09-07
> Status: **DB_6_VOICE_USER_USABLE_V1_PASS / DB_7_FEEDBACK_RUNTIME_INITIAL_PASS**

> **2026-09-08 架构更正**：本文件中的 Work Voice Journey 只保留为历史“AI 能通过 Runtime 取题并写回”的验证证据；当前产品不依赖 Work Voice。正式架构是 `AI Session + interview-training Skill + Interview Runtime/DB`，用户自行切换文字/语音 UI。`/interview/continue` 为正式通用入口，`/voice/continue` 仅兼容。

## 1. DB-6 最终裁决

用户已在 ChatGPT Desktop Work Voice 中完成真实语音 Journey：

`Voice → localhost Runtime Host → DB-5 Runtime → SQLite → Formal Corpus → spoken answer → observation writeback`

真实默认数据库已升级到 migration 003，`integrity_check=ok / foreign_key_check=0`，Formal Corpus 为 21 Family / 42 canonical questions。

真实 Voice Session 已留下 `mode=VOICE / route.ai-agent-devtools / scene.tech1` 的 Session、DB 选出的第一题和回答摘要，证明语音不是只通话，而是在消费 Interview DB。

本次真实 Voice 暴露的缺口：首轮后模型曾自由连续追问，没有每轮先写 Runtime。该缺口已在 DB-6 adapter 修复：新增 `/voice/continue`，强制一次调用内先 `observe_answer`，再由 `next_turn` 返回下一道 Runtime-owned `primaryQuestion`；Skill 同步禁止模型自行编造平行追问。
## 2. DB-6 验收证据

- `app/server/interview-runtime-host.ts`：persistent localhost Host + `/voice/continue`。
- `skills/interview-training/SKILL.md`：Runtime-first、自然语言 route/scene、禁止陪练式提示、下一题必须来自 Runtime。
- DB-6/DB-5 联合 targeted：11/11 PASS。
- DB-6 + DB-7 联合 targeted：18/18 PASS。
- 默认 Host 热路径保留；单次 CLI 仅作为 debug/fallback，不进入 Voice 热路径。

因此：**VOICE_USER_USABLE_V1 = PASS**。用户已明确本次真实语音证明成功，不再把额外麦克风复测作为 Gate。

## 3. DB-7 初始能力

新增 `app/server/interview-feedback.ts`：

- `interview.record_human_feedback`
- `interview.finish_session`

A-F 固定映射：`A→REINFORCE / B→BLUEPRINT / C→ADD_EVIDENCE / D→LEARNING / E→TRADEOFF / F→RESUME`。

反馈只生成 `interview_feedback_events + OPEN interview_training_events`；不会自动修改 Career Facts、Evidence、Story、Resume、Blueprint 或 Learning。

DB-7 targeted 为 4/4 PASS，覆盖 Mock debrief、人工真实面试 observation、A-F 全映射、幂等 Session close。
