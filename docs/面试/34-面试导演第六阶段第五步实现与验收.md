# Interview Director P6-5｜Archetype + Loop 实现与验收

> 日期：2026-09-08
> 状态：**PASS / CONFIGURATION_LAYER_COMPLETE**

## 实现结论

P6-5 没有新增 migration，也没有新增 Director Tool。新增/冻结的是可执行配置层：

- 5 类 `InterviewerArchetype`：`ENGINEER_SCREEN / SENIOR_STAFF_TECH / HIRING_MANAGER / FDE_BUSINESS_ENGINEER / RECRUITER`；
- 10 个现有 Scene 的 `SceneDirectorMandate`；
- 3 条目标 Route 的默认 `InterviewLoopProfile`；
- `Route × Scene → Default DirectorPlan` 构建；
- `OFFICIAL / MULTI_REPORT / USER_REAL` 证据级 Override；
- Persona 只允许改 pressure / hint / interruption / probe depth / timebox，不允许改评价 Authority。

`interview.director_prepare_session` 未传显式 Plan 时，会由 Session 的 route/scene 自动生成默认 Plan；显式 Plan 继续保持最高优先级和向后兼容。## 验收

- P6-5 targeted：`10/10 PASS`；
- P6-3 + P6-4 + P6-5 Director Stage Gate：`27/27 PASS`；
- `git diff --check = PASS`；
- 无新增 migration；
- Director Tool surface 仍为 4 个；
- Legacy / Explicit Plan 路径保持兼容；
- Persona / Override 不得修改 Focus Signal 与评分 Authority。

## 裁决

`P6_5_ARCHETYPE_LOOP = PASS / CONFIGURATION_LAYER_COMPLETE / P6_6_REALISM_EVAL_READY`。

下一阶段：`P6-6 Realism Evaluation / Transcript Fixture TDD`。
