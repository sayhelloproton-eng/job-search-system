# 面试导演第六阶段第六步｜测试驱动开发计划

> 日期：2026-09-09
> 状态：**P6_6_TDD_V1 / SDD_BOUND / RED_FIRST**

## 1. RED 测试集

固定测试编号：

1. `TP-DIR6-001` 强回答使当前 Signal sufficient 后停止同 Signal 深挖；
2. `TP-DIR6-002` Ownership ambiguity 产生可证伪 Hypothesis cross-check；
3. `TP-DIR6-003` strong metric claim 无 baseline 时优先 Claim Debt cross-check；
4. `TP-DIR6-004` `truth.*` HIGH risk 优先于普通 P0 coverage；
5. `TP-DIR6-005` explicit unknown 不再对同 Signal `PROBE_DEPTH`；
6. `TP-DIR6-006` scripted-pattern cue 只触发 constraint-change challenge，不改变 Signal；
7. `TP-DIR6-007` hard budget advance/close，且不开新深挖；
8. `TP-DIR6-008` Tech1 / HM / FDE default plan 与 replay target 可区分；
9. `TP-DIR6-009` candidate-visible next TurnPack 不泄露 Director hidden metadata；
10. `TP-DIR6-010` 相同 plan/transcript 的 semantic Decision replay 稳定；
11. `TP-DIR6-011` Persona style override 不改变 focus/score authority；
12. `TP-DIR6-012` Realism summary 对任一关键 fixture fail 都 fail-closed，不用平均分掩盖。

## 2. 预期 RED

基于 P6-1 合同与当前 P6-3 实现，至少两项应先 RED：
- `TP-DIR6-004`：当前 `decideInterviewDirector` 尚未读取 HIGH/FATAL `truth.*` risk；
- `TP-DIR6-006`：当前 Director 尚无结构化 anti-script reasoning challenge cue。

如果这两项在未改代码前意外 PASS，必须先检查测试是否绕过真实 Director；不得为了宣布 RED 而造假失败。

## 3. 最小实现边界

允许修改：
- `app/server/interview-director.ts`：补 frozen policy 缺口；
- `app/server/interview-runtime-cli.ts` / `interview-runtime-host.ts`：仅在需要传递新增 structured cue 时做最小透传；
- 新增 `app/server/interview-director-realism-eval.ts`：semantic projection + fail-closed summary；
- 新增 `app/tests/interview-director-realism.test.ts`：固定 replay fixtures。

不允许：migration、Question Corpus 改写、Career Facts/Resume mutation、新 Director Tool。

## 4. Regression Gate

P6-6 targeted 通过后至少运行：

```text
interview-director.test.ts
interview-director-runtime.test.ts
interview-director-profiles.test.ts
interview-runtime-host.test.ts
interview-voice-runtime.test.ts
interview-retrieval.test.ts
```

若 production runtime 有修改，再运行完整 `npm test`。

## 5. 验收

P6-6 机械 Gate 必须：
- 12/12 fixture/test PASS；
- P6-3～P6-5 无回归；
- `git diff --check` PASS；
- 无新增 migration；
- Director Tool surface 仍为 4；
- Career Facts / Resume Evidence 0 mutation；
- Application external action 0。
