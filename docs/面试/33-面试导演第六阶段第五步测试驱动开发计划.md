# Interview Director P6-5｜Archetype + Loop Templates TDD

> 日期：2026-09-08
> 状态：**TDD_PLAN_V1 / CONFIGURATION_LAYER_ONLY**

## 目标

把 P6-2 Director Corpus 的 Reality Pattern 变成可执行默认配置，不新增数据库表、不新增平行题库。

## RED Gate

1. 固定 5 类 `InterviewerArchetype`，风格参数不包含评分权重；
2. 10 个现有 Scene 均有明确 Mandate / Focus Signal / Archetype；
3. 三条 Route 均有默认 Interview Loop；
4. AI Agent Tech1、Advanced FE Deep Dive、ToB/FDE Case 生成不同 Plan；
5. `director_prepare_session` 未传 plan 时可从 Session route/scene 构造默认 Plan；
6. 显式 plan 仍优先，兼容 P6-3/P6-4；
7. Override 只有 `OFFICIAL / MULTI_REPORT / USER_REAL` 可应用；
8. Override 不能新增未知 Signal、不能修改 Career Facts；
9. Persona 只能影响 pressure/hint/interruption/probe depth，不改变同一 Mission 的评价目标；
10. 三 Route 的默认 Loop 可被 Runtime 查询用于后续完整模拟。