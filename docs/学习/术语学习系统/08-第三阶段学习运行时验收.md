# T3｜Terminology Learning Runtime 实现与验收

> 日期：2026-09-08
> 状态：**T3_RUNTIME_PASS / MIGRATION_007 / SIX_OPERATIONS / CAREER_FACTS_IMMUTABLE**

## 1. 实现

新增：
- `app/db/migrations/007_learning_terminology_v1.sql`；
- `app/server/learning-terminology-runtime.ts`；
- `app/tests/learning-terminology-runtime.test.ts`。

Runtime 暴露六个操作：
`term_lookup / search_terms / plan_term_path / record_term_progress / get_term_progress / list_due_reviews`。

静态知识继续由 `terms-v1.json + aliases-v1.json + term-graph-v1.json` 提供，SQLite 只保存个人学习快照和 append-only progress history。
## 2. Gate

- T3 targeted：`7/7 PASS`；
- DB migration tests：PASS；
- existing Learning Bridge：`4/4 PASS`；
- existing Runtime CLI：`3/3 PASS`；
- Context Governance stale-current assertion 已校正；
- Context + T3 isolated：`13/13 PASS`；
- `git diff --check`：PASS。

第一次 truth-boundary 测试失败来自既有 corpus lazy initialization 发生在 baseline 之后；修正测试顺序后通过，没有修改产品逻辑。

## 3. 裁决

`T3_LEARNING_RUNTIME = PASS / STATIC_KNOWLEDGE_JSON_AUTHORITY / SQLITE_PROGRESS_ONLY / APPEND_ONLY_HISTORY / REQUEST_ID_IDEMPOTENT / DUE_REVIEW / NO_CAREER_FACT_MUTATION`。

下一 Gate：`T4_INTERVIEW_JD_FEEDBACK_INTEGRATION`。