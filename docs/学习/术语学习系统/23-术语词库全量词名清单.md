# 术语词库全量词名清单｜1407 Canonical Terms

> 这是给人直接查看“都有哪些词”的唯一词名清单；一词一行，不按多链重复。Canonical Truth 仍以 `data/terms-v1.json` 为准。

0001. **A2A** — agent
0002. **A2A Boundary** — agent
0003. **A2A Task** — agent
0004. **ABAC** — backend
0005. **AbortController** — frontend
0006. **AbortSignal** — frontend
0007. **Abstain** — agent
0008. **Acceptance Criteria** — fullstack
0009. **Access Control** — agent
0010. **Accessibility** — frontend
0011. **Accessibility Tree** — agent
0012. **ACID** — backend
0013. **Ack** — backend
0014. **Act** — agent
0015. **Action** — agent / frontend
0016. **Action Schema** — agent
0017. **Admission Control** — agent
0018. **Adoption** — agent
0019. **Agent** — agent
0020. **Agent Application** — agent
0021. **Agent Card** — agent
0022. **Agent Economics** — agent
0023. **Agent Event Stream** — fullstack
0024. **Agent Explosion** — agent
0025. **Agent Loop** — agent
0026. **Agent Registry** — agent
0027. **Agent Routing** — agent
0028. **Agent Run** — fullstack
0029. **Agent Runtime** — agent
0030. **Agent State** — fullstack
0031. **Agent Trace** — agent
0032. **Agent-as-Tool** — agent
0033. **Agent-Workflow Layer** — agent
0034. **Agentic Decision** — agent
0035. **Agentic RAG** — agent
0036. **Agentic Retrieval** — agent
0037. **Aging** — agent
0038. **AI Feature** — agent
0039. **AI Platform** — agent
0040. **Air-gapped Deployment** — fullstack
0041. **Alert** — backend
0042. **Alert Fatigue** — backend
0043. **Alignment** — agent
0044. **Ambiguous** — agent
0045. **API** — shared
0046. **API Client Generation** — fullstack
0047. **API Contract** — fullstack
0048. **API Gateway** — fullstack
0049. **API Gateway Policy** — backend
0050. **API Key** — backend
0051. **API Versioning** — backend
0052. **Approval** — agent
0053. **Approval UI** — fullstack
0054. **APPROVAL_REQUIRED** — agent
0055. **Architecture by Evidence** — agent
0056. **Architecture Choice** — agent
0057. **Architecture Fitness Function** — agent
0058. **Arguments** — agent
0059. **ARIA** — frontend
0060. **Artifact** — agent / fullstack
0061. **Artifact Eval** — agent
0062. **Artifact Reference** — agent
0063. **Assessment** — agent
0064. **Assistant Message** — agent
0065. **Async** — shared
0066. **Async Job** — fullstack
0067. **Async Tool** — agent
0068. **async_hooks** — backend
0069. **async/await** — frontend — alias: async-await
0070. **Asynchronous Subagent** — agent
0071. **AsyncLocalStorage** — backend
0072. **At-least-once** — backend
0073. **At-most-once** — backend
0074. **Atomicity** — shared
0075. **Attempt** — agent
0076. **Attention** — agent
0077. **Attention Key** — agent
0078. **Audit** — agent
0079. **Audit Actor** — fullstack
0080. **Audit Trail** — fullstack
0081. **Authentication** — agent / backend / shared
0082. **Authentication Flow** — fullstack
0083. **Authoritative Source** — agent
0084. **Authority** — agent
0085. **Authorization** — agent / backend / shared
0086. **Authorization Code Flow** — backend
0087. **Authorization Flow** — fullstack
0088. **Auto-instrumentation** — backend / shared
0089. **AutoGen** — agent
0090. **Automatic Batching** — frontend
0091. **Autoregressive Generation** — agent
0092. **Autovacuum** — backend
0093. **Availability** — shared
0094. **Availability Filter** — agent
0095. **B-tree** — backend
0096. **Babel** — frontend
0097. **Backend Span** — fullstack
0098. **Background Job** — backend
0099. **Background Run** — fullstack
0100. **Backoff** — agent
0101. **Backpressure** — backend / shared
0102. **Backward Compatibility** — fullstack
0103. **Baggage** — backend / fullstack / shared
0104. **Base Model** — agent
0105. **Batching** — agent
0106. **Benchmark** — agent
0107. **BFCache** — frontend
0108. **BFF** — fullstack — alias: Backend-for-Frontend
0109. **Bitmap Scan** — backend
0110. **Blackboard** — agent
0111. **Blast Radius** — agent
0112. **Block Formatting Context** — frontend
0113. **Blocked** — agent
0114. **Blocking** — backend
0115. **Bloom Filter** — backend
0116. **Blue-Green** — fullstack
0117. **Blue-Green Deployment** — backend
0118. **BM25** — agent
0119. **Boundary Validation** — fullstack
0120. **Bounded Delegation** — agent
0121. **Box Model** — frontend
0122. **Branch** — agent
0123. **Brownout** — backend
0124. **Browser** — frontend
0125. **Browser Agent** — agent
0126. **Browser Error** — fullstack
0127. **Budget** — agent
0128. **Buffer** — backend
0129. **Build Artifact** — fullstack
0130. **Build vs Buy** — agent
0131. **Build-time Configuration** — fullstack
0132. **Build-time Integration** — frontend
0133. **Bulkhead** — backend
0134. **Bundle Analysis** — frontend
0135. **Bundle Budget** — frontend
0136. **Bundler** — frontend
0137. **Burn Rate** — backend
0138. **Business Outcome** — agent
0139. **Cache** — backend / shared
0140. **Cache Avalanche** — backend
0141. **Cache Coherence** — backend
0142. **Cache Components** — frontend / fullstack
0143. **Cache Hit** — frontend
0144. **Cache Invalidation** — backend / fullstack
0145. **Cache Key** — fullstack
0146. **Cache Miss** — frontend
0147. **Cache Penetration** — backend
0148. **Cache Purge** — fullstack
0149. **Cache Stampede** — backend
0150. **Cache Storage** — frontend
0151. **Cache Tag** — frontend / fullstack — alias: cacheTag
0152. **Cache-aside** — backend
0153. **Cache-Control** — frontend
0154. **Cache-through** — backend
0155. **cacheLife** — frontend
0156. **Call Intent** — agent
0157. **Call Stack** — frontend
0158. **Canary** — fullstack
0159. **Canary Deployment** — backend
0160. **Canary Eval** — agent
0161. **Cancellation** — agent / backend / shared
0162. **Cancellation Propagation** — fullstack
0163. **Candidate Fusion** — agent
0164. **CAP Theorem** — backend
0165. **Capability** — agent
0166. **Capability Contract** — agent
0167. **Capability Declaration** — agent
0168. **Capability Discovery** — agent
0169. **Capability Interoperability** — agent
0170. **Capability Layer** — agent
0171. **Capability Map** — agent
0172. **Capability Mapping** — fullstack
0173. **Capability Metadata** — agent
0174. **Capability Negotiation** — agent
0175. **Capability Registry** — agent
0176. **Cardinality** — shared
0177. **Cascade** — frontend
0178. **Cascading Failure** — backend
0179. **Causal Mask** — agent
0180. **CD** — fullstack
0181. **CDC** — backend
0182. **CDN** — frontend / fullstack
0183. **CDN Invalidation** — fullstack
0184. **Chain** — agent
0185. **Chat Model** — agent
0186. **Chat Template** — agent
0187. **Chat UI** — fullstack
0188. **Checkpoint** — agent / backend / fullstack
0189. **Child Process** — backend
0190. **Child Task** — agent
0191. **Chunk** — agent / frontend
0192. **Chunk Overlap** — agent
0193. **Chunked Response** — fullstack
0194. **Chunker** — agent
0195. **Chunking** — agent
0196. **CI** — fullstack
0197. **Circuit Breaker** — backend
0198. **Circuit Breaking** — backend
0199. **Circular Dependency** — frontend
0200. **Citation** — agent / fullstack
0201. **Claim** — agent
0202. **Claude Code Skill** — agent
0203. **Cleaner** — agent
0204. **Clickjacking** — frontend
0205. **Client** — shared
0206. **Client Component Boundary** — fullstack
0207. **Client Components** — frontend
0208. **Client Credential Flow** — backend
0209. **Client Data Fetching** — fullstack
0210. **Client Disconnect** — fullstack
0211. **Client State** — fullstack
0212. **Client Timeout** — backend
0213. **Client-only Module** — fullstack
0214. **Client-Server** — fullstack
0215. **Clock Skew** — backend
0216. **Closed-loop Self-confirmation** — agent
0217. **Closure** — frontend
0218. **Cloud Model** — agent
0219. **CLS** — frontend
0220. **cluster** — backend
0221. **Cluster Autoscaler** — backend
0222. **Code** — agent
0223. **Code Generation** — fullstack
0224. **Code Splitting** — frontend
0225. **Coding Agent** — agent
0226. **COEP** — frontend
0227. **Cold Start** — fullstack
0228. **Collaboration** — agent
0229. **Commit** — frontend
0230. **Committed Fact** — agent
0231. **Compare-and-set** — agent
0232. **Compatibility Window** — fullstack
0233. **Compensation** — agent / backend
0234. **Compile-time Type** — fullstack
0235. **Compiler** — agent / frontend
0236. **Completeness** — agent
0237. **Component** — frontend
0238. **Component Library** — frontend
0239. **Component Test** — frontend
0240. **Composite** — frontend
0241. **Compositing Layer** — frontend
0242. **Compound Component** — frontend
0243. **Compression** — backend
0244. **Compute** — agent
0245. **Concurrency** — shared
0246. **Concurrency Limit** — agent
0247. **Conditional Request** — backend
0248. **Conditional Type** — frontend
0249. **Config** — backend / fullstack
0250. **ConfigMap** — backend
0251. **Conflict Gate** — agent
0252. **Conflict Rate** — agent
0253. **Conflict Resolution** — fullstack
0254. **Connection** — shared
0255. **Connection Pool** — backend / shared
0256. **Connection Reuse** — backend
0257. **Connector** — fullstack
0258. **Consensus** — backend
0259. **Consent** — agent
0260. **Consistency** — backend
0261. **Consistent Hashing** — backend
0262. **Consumer** — backend
0263. **Consumer-driven Contract** — fullstack
0264. **Container** — backend
0265. **Container Query** — frontend
0266. **Container Runtime** — fullstack
0267. **Containing Block** — frontend
0268. **Content Negotiation** — backend
0269. **Content-Type** — backend
0270. **Context Assembly** — agent
0271. **Context Compaction** — agent
0272. **Context Compression** — agent
0273. **Context Engineering** — agent
0274. **Context Evidence Hit** — agent
0275. **Context Isolation** — agent
0276. **Context Propagation** — backend / fullstack / shared
0277. **Context Provider** — frontend
0278. **Context Window** — agent
0279. **Contract** — backend
0280. **Contract Test** — fullstack
0281. **Contract-first** — fullstack
0282. **Control Plane** — agent
0283. **Controlled Component** — frontend
0284. **Controller** — agent
0285. **Conversation** — fullstack
0286. **Conversation History** — agent
0287. **Conversation Replay** — agent
0288. **Cookie** — frontend / fullstack
0289. **Cookie Session** — backend
0290. **Cookie-based Session** — fullstack
0291. **COOP** — frontend
0292. **Coordination Calls** — agent
0293. **Coordination Intensity** — agent
0294. **Coordination Tax** — agent
0295. **Cork** — backend
0296. **Correlation ID** — backend / fullstack
0297. **CORS** — frontend / fullstack
0298. **CORS Policy** — fullstack
0299. **CORS Preflight** — frontend
0300. **Cosine Similarity** — agent
0301. **Cost Attribution** — agent
0302. **Cost per Success** — agent
0303. **Counterexample** — agent
0304. **Counterfactual** — agent
0305. **Coverage** — agent
0306. **CPU Profile** — backend
0307. **CPU-bound** — backend
0308. **CQRS** — backend
0309. **Critical CSS** — frontend
0310. **Critical Error** — agent
0311. **Critical Error Rate** — agent
0312. **Critical Rendering Path** — frontend
0313. **Critical Section** — shared
0314. **CronJob** — backend
0315. **Cross-Encoder** — agent
0316. **Cross-origin Isolation** — frontend
0317. **Cross-Runtime Boundary** — agent
0318. **Cross-service Timeline** — fullstack
0319. **CSP** — frontend
0320. **CSP Hash** — frontend
0321. **CSP Nonce** — frontend
0322. **CSR** — frontend
0323. **CSRF** — backend / frontend
0324. **CSRF Protection** — fullstack
0325. **CSRF Token** — fullstack
0326. **CSS Containment** — frontend
0327. **CSS Variable** — frontend
0328. **CSSOM** — frontend
0329. **CTE** — backend
0330. **Current Truth** — agent
0331. **Cursor** — backend
0332. **Custom Elements** — frontend
0333. **Custom Property** — frontend
0334. **Customer Data Boundary** — fullstack
0335. **DaemonSet** — backend
0336. **DAG** — agent
0337. **Data Consistency** — fullstack
0338. **Data Fetching** — frontend
0339. **Data Handling Restriction** — agent
0340. **Data Parallelism** — agent
0341. **Data Plane** — agent
0342. **Database Migration** — fullstack
0343. **Dead-letter Queue** — backend
0344. **Deadline** — shared
0345. **Deadlock** — backend / shared
0346. **Decision** — agent
0347. **Decision Record** — agent
0348. **Declaration File** — frontend
0349. **Decoding** — agent
0350. **Deep Research** — agent
0351. **Delegated Authorization** — fullstack
0352. **Delegation** — agent
0353. **Delivery Gate** — fullstack
0354. **Delivery Semantics** — backend
0355. **Denormalization** — backend
0356. **Dense Retrieval** — agent
0357. **Dependency** — agent
0358. **Dependency Graph** — frontend
0359. **Deployment** — fullstack
0360. **Derived Index** — agent
0361. **Deserialization** — shared
0362. **Design System** — frontend
0363. **Design Token** — frontend
0364. **Deterministic Control** — agent
0365. **Deterministic Eval** — agent
0366. **Deterministic Evaluator** — agent
0367. **diagnostics_channel** — backend
0368. **Dify** — agent
0369. **Dirty Read** — backend
0370. **Discovery** — agent
0371. **Discovery Rule** — agent
0372. **Discriminated Union** — frontend
0373. **Distributed Cache** — backend
0374. **Distributed Lock** — backend
0375. **Distributed Trace** — fullstack
0376. **DNS** — shared
0377. **Docker Image** — backend
0378. **Dockerfile** — backend
0379. **Document** — agent
0380. **DOM** — frontend
0381. **DOM XSS** — frontend
0382. **DOMContentLoaded** — frontend
0383. **DPO** — agent
0384. **DTO** — fullstack
0385. **Duplex** — backend
0386. **Duplicate Submission** — fullstack
0387. **Durable Execution** — agent
0388. **Durable State Machine** — agent
0389. **Dynamic Agent Creation** — agent
0390. **Dynamic Import** — frontend
0391. **Dynamic Policy** — agent
0392. **Dynamic Rendering** — frontend / fullstack
0393. **E2E** — frontend / fullstack
0394. **Edge** — agent / fullstack
0395. **Edge Function** — fullstack
0396. **Edge Runtime** — fullstack
0397. **Effect** — agent
0398. **Effect Boundary** — agent
0399. **Effect Eval** — agent
0400. **Effect Lifecycle** — agent
0401. **Effect Semantics** — agent
0402. **Elicitation** — agent
0403. **Embedding** — agent
0404. **Embedding Matrix** — agent
0405. **Embedding Model** — agent
0406. **Environment** — fullstack
0407. **Environment Contract** — agent
0408. **Environment Variable** — backend
0409. **Environment-specific Harness** — agent
0410. **Ephemeral Agent** — agent
0411. **Episodic Memory** — agent
0412. **Error Accumulation** — agent
0413. **Error Boundary** — frontend
0414. **Error Budget** — backend / shared
0415. **Error Reporting** — frontend
0416. **Escalation** — agent
0417. **ESM** — frontend
0418. **ETag** — backend
0419. **ETag Concurrency** — fullstack
0420. **Eval** — agent — alias: Evaluation
0421. **Evaluator Defect** — agent
0422. **Event** — agent / frontend
0423. **Event Bubbling** — frontend
0424. **Event Bus** — agent
0425. **Event Capturing** — frontend
0426. **Event ID** — fullstack
0427. **Event Loop** — backend / frontend / shared
0428. **Event Loop Lag** — backend
0429. **Event Ordering** — agent
0430. **Event Sourcing** — backend
0431. **Event-driven** — shared
0432. **Event-driven Collaboration** — agent
0433. **EventEmitter** — backend
0434. **Eventual Consistency** — backend
0435. **Eventual UI Consistency** — fullstack
0436. **Eviction** — backend
0437. **Eviction Policy** — backend
0438. **Evidence** — fullstack
0439. **Evidence Conflict** — agent
0440. **Evidence-bound Approval** — agent
0441. **Exactly-once Claim** — backend
0442. **Exception Handling** — agent
0443. **Execution Environment** — agent
0444. **Executor** — agent
0445. **Exit Condition** — agent
0446. **Expand-and-contract Migration** — fullstack
0447. **Expert** — agent
0448. **EXPLAIN** — backend
0449. **EXPLAIN ANALYZE** — backend
0450. **Exponential Backoff** — backend
0451. **External Readback** — agent / fullstack
0452. **External Store** — frontend
0453. **External Truth** — agent
0454. **Failure Boundary** — fullstack
0455. **Failure Class** — agent
0456. **Failure Containment** — agent
0457. **Failure Contract** — agent
0458. **Failure Detector** — backend
0459. **Failure Domain** — shared
0460. **Failure Recovery** — agent
0461. **Failure Slice** — agent
0462. **Failure Taxonomy** — agent
0463. **Fairness** — shared
0464. **Faithfulness** — agent
0465. **Fallback** — agent
0466. **FAST Profile** — agent
0467. **Fault Tolerance** — shared
0468. **FCP** — frontend
0469. **Feature Flag** — fullstack
0470. **Feature Slice** — frontend
0471. **Feedback Density** — agent
0472. **Feedback Loop** — agent
0473. **Fencing Token** — backend
0474. **Fetch** — frontend
0475. **Fetch Priority** — frontend
0476. **Few-shot** — agent
0477. **FFN** — agent
0478. **Fiber** — frontend
0479. **File Descriptor** — shared
0480. **Final Answer Eval** — agent
0481. **Final Result** — fullstack
0482. **Flex Formatting Context** — frontend
0483. **Flow Control** — shared
0484. **Font Loading** — frontend
0485. **Foreign Key** — backend
0486. **Formatting Context** — frontend
0487. **Forward Compatibility** — fullstack
0488. **Foundation Model** — agent
0489. **Fresh Evidence** — agent
0490. **Freshness** — agent
0491. **Frontend Span** — fullstack
0492. **Frontend Trace** — frontend
0493. **Full Re-index** — agent
0494. **Function Calling** — agent
0495. **Garbage Collection** — frontend
0496. **Gateway API** — backend
0497. **Generation Eval** — agent
0498. **Generator** — frontend
0499. **Generic** — frontend
0500. **Goal** — agent
0501. **Goal Drift** — agent
0502. **Goal Success** — agent
0503. **Golden Set** — agent
0504. **Governance** — agent
0505. **Graceful Degradation** — backend
0506. **Graceful Shutdown** — backend
0507. **Grader** — agent
0508. **Graph** — agent
0509. **GraphQL** — backend
0510. **Grid Formatting Context** — frontend
0511. **Grounding** — agent
0512. **gRPC** — backend
0513. **Guardrail** — agent
0514. **Hallucination** — agent
0515. **Handoff** — agent
0516. **Handoff Context** — agent
0517. **Handoff Proposal** — agent
0518. **Handshake** — agent
0519. **Harness** — agent
0520. **Head Sampling** — backend / shared
0521. **Head-of-line Blocking** — backend
0522. **Header** — backend
0523. **Headless Component** — frontend
0524. **Health Check** — backend
0525. **Heap** — frontend
0526. **Heap Snapshot** — backend
0527. **Heartbeat** — backend / fullstack / shared
0528. **Higher-order Component** — frontend
0529. **HighWaterMark** — backend
0530. **History** — agent
0531. **HMR** — frontend
0532. **Hoisting** — frontend
0533. **Hook** — agent / frontend
0534. **Hot Key** — backend
0535. **HPA** — backend
0536. **HTML Parsing** — frontend
0537. **HTTP** — shared
0538. **HTTP Method** — backend
0539. **HTTP/1.1** — shared
0540. **HTTP/2** — backend / shared
0541. **HTTP/3** — backend / shared
0542. **HttpOnly** — frontend
0543. **Human Approval** — fullstack
0544. **Human Intervention** — agent
0545. **Human Load** — agent
0546. **Human-in-the-loop** — agent
0547. **Human-in-the-loop UI** — fullstack
0548. **Human-on-the-loop** — agent
0549. **Hybrid Retrieval** — agent
0550. **Hydration** — frontend / fullstack
0551. **Hydration Boundary** — fullstack
0552. **Hydration Cost** — frontend
0553. **Hydration Mismatch** — frontend
0554. **Hypothesis** — agent
0555. **I/O-bound** — backend
0556. **IAM** — agent
0557. **Idempotency** — agent / shared
0558. **Idempotency Boundary** — fullstack
0559. **Idempotency Key** — agent / backend
0560. **Idempotent Consumer** — agent
0561. **Identity** — agent
0562. **Identity Continuity** — agent
0563. **iframe Sandbox** — frontend
0564. **Image Optimization** — frontend
0565. **Immutable Artifact** — fullstack
0566. **Immutable Version** — agent
0567. **Impersonation** — fullstack
0568. **Implemented Candidate** — agent
0569. **In-context Learning** — agent
0570. **Inbox Pattern** — backend
0571. **Incident Containment** — agent
0572. **Incremental Indexing** — agent
0573. **Independent Agent** — agent
0574. **Index** — backend
0575. **Index Scan** — backend
0576. **IndexedDB** — frontend
0577. **Indexing** — agent
0578. **Inference** — agent
0579. **Inference-time Compute** — agent
0580. **Information Funnel** — agent
0581. **Infrastructure Layer** — agent
0582. **Ingestion** — agent
0583. **Ingress** — backend
0584. **Inheritance** — frontend
0585. **Init Container** — backend
0586. **INP** — frontend
0587. **Input Contract** — agent
0588. **Instruct Model** — agent
0589. **Instruction** — agent
0590. **Instruction File** — agent
0591. **Instruction Hierarchy** — agent
0592. **Instruction Tuning** — agent
0593. **Instrumentation** — backend / shared
0594. **Integrated Environment Evidence** — agent
0595. **Integration Contract** — fullstack
0596. **Integration Test** — frontend / fullstack
0597. **Interaction Latency** — frontend
0598. **Intermediate Result** — agent
0599. **Intersection Type** — frontend
0600. **IntersectionObserver** — frontend
0601. **Intrinsic Size** — frontend
0602. **Invalidate** — agent
0603. **Invalidation** — fullstack
0604. **Inverted Index** — agent
0605. **Isolation Level** — backend
0606. **ISR** — frontend
0607. **Iterable** — frontend
0608. **Iterator** — frontend
0609. **JavaScript Execution Cost** — frontend
0610. **Jitter** — backend
0611. **Job** — backend
0612. **Join** — backend
0613. **JSON** — shared
0614. **JSON Schema** — agent / fullstack
0615. **JSON-RPC** — agent / shared
0616. **JWE** — backend
0617. **JWK** — backend
0618. **JWKS** — backend
0619. **JWS** — backend
0620. **JWT** — backend / fullstack
0621. **Keep-Alive** — backend / shared
0622. **Key Derivation Function** — backend
0623. **Keyword** — agent
0624. **Kill Switch** — fullstack
0625. **Knowledge Base** — agent
0626. **Knowledge Distillation** — agent
0627. **Knowledge Distribution** — agent
0628. **Knowledge Gap** — agent
0629. **Knowledge Layer** — agent
0630. **Knowledge Source** — agent
0631. **Kubernetes Deployment** — backend
0632. **Kubernetes RBAC** — backend
0633. **KV Cache** — agent
0634. **Lamport Clock** — backend
0635. **LangGraph** — agent
0636. **Late Result** — agent
0637. **Latency Budget** — agent
0638. **Layer** — backend
0639. **LayerNorm** — agent
0640. **Layout** — frontend
0641. **Layout Shift** — frontend
0642. **Lazy Loading** — frontend
0643. **LCP** — frontend
0644. **Leader Election** — backend
0645. **Learned Reranker** — agent
0646. **Lease** — agent / shared
0647. **Least Memory Scope** — agent
0648. **Least Privilege** — agent
0649. **Lexical Environment** — frontend
0650. **Lexical Retrieval** — agent
0651. **LFU** — backend
0652. **libuv** — backend
0653. **Lifecycle** — agent
0654. **Lifetime** — agent
0655. **Linter** — agent
0656. **Liveness** — backend
0657. **Liveness Probe** — backend
0658. **LlamaIndex** — agent
0659. **LLM** — agent
0660. **LLM-as-Judge** — agent
0661. **Load Balancer** — shared
0662. **load Event** — frontend
0663. **Load Shedding** — backend
0664. **Loader** — agent
0665. **Local Cache** — backend
0666. **Local Hypothesis** — agent
0667. **localStorage** — frontend
0668. **Lock** — backend
0669. **Log** — backend / shared — alias: Logs
0670. **Log Correlation** — backend / fullstack
0671. **Logical Clock** — backend
0672. **Logical Replication** — backend
0673. **Login Flow** — fullstack
0674. **Logits** — agent
0675. **Long Animation Frame** — frontend
0676. **Long Polling** — backend
0677. **Long Task** — frontend
0678. **Long-running Request** — fullstack
0679. **Long-term Memory** — agent
0680. **Lost Update** — backend
0681. **Lost Update Prevention** — fullstack
0682. **LRU** — backend
0683. **LSN** — backend
0684. **Machine Interruption** — agent
0685. **Machine-verifiable Feedback** — agent
0686. **Main Thread** — frontend
0687. **Map** — frontend
0688. **Map-Reduce** — agent
0689. **Mapped Type** — frontend
0690. **Mapping** — agent
0691. **Marginal Utility** — agent
0692. **Maturity Path** — agent
0693. **Max-content** — frontend
0694. **MCP** — agent
0695. **MCP Client** — agent
0696. **MCP Host** — agent
0697. **MCP Prompt** — agent
0698. **MCP Resource** — agent
0699. **MCP Result** — agent
0700. **MCP Server** — agent
0701. **MCP Session ID** — agent
0702. **MCP Task** — agent
0703. **MCP Tasks Extension** — agent
0704. **MCP Tool** — agent
0705. **Media Query** — frontend
0706. **Memoization** — frontend
0707. **Memory** — agent
0708. **Memory Candidate** — agent
0709. **Memory Conflict** — agent
0710. **Memory Deletion** — agent
0711. **Memory Leak** — frontend
0712. **Memory Promotion** — agent
0713. **Memory Retrieval** — agent
0714. **Message Broker** — backend
0715. **Metadata** — agent
0716. **Metadata Filter** — agent
0717. **Metric** — backend / shared — alias: Metrics
0718. **Metric Cardinality** — backend
0719. **Micro-frontend** — frontend
0720. **Micro-frontend Orchestration** — frontend
0721. **Microsoft Agent Framework** — agent
0722. **Microtask** — frontend
0723. **Middleware** — fullstack
0724. **Migration** — backend
0725. **Min-content** — frontend
0726. **Minimal Sufficient Context** — agent
0727. **Minimal Transfer** — agent
0728. **Minimum Viable AI System** — agent
0729. **Mock** — frontend
0730. **Modality** — agent
0731. **Model Capability** — agent
0732. **Model Cluster** — agent
0733. **Model Gateway** — agent
0734. **Model Intelligence Layer** — agent
0735. **Model Invocation** — agent
0736. **Model Parallelism** — agent
0737. **Model Routing** — agent
0738. **Model-visible Capability Set** — agent
0739. **Module** — frontend
0740. **Module Boundary** — frontend
0741. **Module Federation** — frontend
0742. **Module Resolution** — frontend
0743. **MoE** — agent
0744. **MoE Router** — agent
0745. **Monorepo** — frontend / fullstack
0746. **mTLS** — backend
0747. **Multi-Agent** — agent
0748. **Multi-Head Attention** — agent
0749. **Multi-model System** — agent
0750. **Multi-stage Retrieval** — agent
0751. **Multi-tenancy** — agent
0752. **Multimodal Model** — agent
0753. **Multipart** — backend
0754. **Mutation** — fullstack
0755. **Mutation Idempotency** — fullstack
0756. **MutationObserver** — frontend
0757. **Mutex** — shared
0758. **Mutual Exclusion** — agent
0759. **Mutual Resource** — agent
0760. **MVCC** — backend
0761. **Namespace** — agent / backend
0762. **Narrowing** — frontend
0763. **Native Coordination** — agent
0764. **Navigation Lifecycle** — frontend
0765. **Navigation Timing** — frontend
0766. **Negative Cache** — backend
0767. **Network** — backend
0768. **Network Partition** — backend
0769. **Next-step Decision** — agent
0770. **Next-Token Prediction** — agent
0771. **Node** — agent
0772. **Node Affinity** — backend
0773. **Node API** — fullstack
0774. **Node Runtime** — fullstack
0775. **Node.js** — backend
0776. **Non-blocking I/O** — backend
0777. **Non-repeatable Read** — backend
0778. **Normalization** — backend
0779. **Normalizer** — agent
0780. **Notification** — agent
0781. **OAuth 2.0** — backend / fullstack
0782. **OAuth Grant** — backend
0783. **Observability** — fullstack / shared
0784. **Observation** — agent
0785. **Observation Schema** — agent
0786. **Offline Eval** — agent
0787. **On-device Model** — agent
0788. **On-prem** — fullstack
0789. **One-shot** — agent
0790. **Online Eval** — agent
0791. **Opaque Execution** — agent
0792. **OpenAI Agents SDK** — agent
0793. **OpenAPI** — fullstack
0794. **OpenID Connect** — backend / fullstack — alias: OIDC
0795. **OpenTelemetry** — backend
0796. **Operation ID** — agent
0797. **Operation Identity** — agent
0798. **Operational Handoff** — fullstack
0799. **Operational Tax** — agent
0800. **Optimistic Lock** — backend
0801. **Optimistic UI** — fullstack
0802. **Optimistic Update** — fullstack
0803. **Optimistic Versioning** — agent
0804. **Organization Context** — fullstack
0805. **Origin** — frontend
0806. **ORM** — backend
0807. **Outbox** — backend
0808. **Outcome Eval** — agent
0809. **Output Contract** — agent
0810. **Overflow** — frontend
0811. **Owner** — agent
0812. **P2P** — agent
0813. **p50 Latency** — agent
0814. **p95 Latency** — agent
0815. **PACELC** — backend
0816. **Package Boundary** — frontend / fullstack
0817. **Pagination** — backend / fullstack
0818. **Paint** — frontend
0819. **Parallel Retrieval** — agent
0820. **Parallel Tool Call** — agent
0821. **Parallelism** — agent / shared
0822. **Parameters** — agent
0823. **Parent Span** — shared
0824. **Parent Task** — agent
0825. **Pareto Frontier** — agent
0826. **Parser** — agent
0827. **Partial Hydration** — frontend
0828. **Partial Prerendering** — frontend / fullstack
0829. **Partial Result** — fullstack
0830. **Partition Pruning** — backend
0831. **Partitioning** — backend
0832. **Password Hashing** — backend
0833. **Paved Road** — agent
0834. **Peer Dependency** — frontend
0835. **Performance API** — frontend
0836. **PerformanceObserver** — frontend
0837. **Permission** — agent / backend
0838. **Permission Filter** — agent
0839. **Permission Filtering** — agent
0840. **Persistent Agent** — agent
0841. **PersistentVolume** — backend
0842. **PersistentVolumeClaim** — backend
0843. **Pessimistic Lock** — backend
0844. **Pessimistic Update** — fullstack
0845. **Phantom Read** — backend
0846. **PKCE** — backend / fullstack
0847. **Plan** — agent
0848. **Plan Version** — agent
0849. **Planner** — agent
0850. **Planning** — agent
0851. **Platform Host** — agent
0852. **Platformization** — agent
0853. **Pod** — backend
0854. **Pod Anti-affinity** — backend
0855. **PodDisruptionBudget** — backend
0856. **Policy** — agent
0857. **Policy Version** — agent
0858. **Polling** — agent
0859. **Polling Fallback** — fullstack
0860. **Port** — shared
0861. **Positioning** — frontend
0862. **Post-training** — agent
0863. **postMessage** — frontend
0864. **Pre-training** — agent
0865. **Precedence** — agent
0866. **Precision** — agent
0867. **Preconnect** — frontend
0868. **Preference Data** — agent
0869. **Prefetch** — frontend
0870. **Prefill** — agent
0871. **Preload** — frontend
0872. **Primary Key** — backend
0873. **Primary Source** — agent
0874. **Primitive** — agent
0875. **Principle of Least Privilege** — backend
0876. **Priority Hints** — frontend
0877. **Priority Queue** — agent
0878. **Private Context** — agent
0879. **Private Deployment** — fullstack
0880. **Probe** — backend
0881. **Procedural Knowledge** — agent
0882. **Procedural Memory** — agent
0883. **Process** — shared
0884. **Process Exit** — backend
0885. **Process Signal** — backend
0886. **Producer** — backend
0887. **Productization** — fullstack
0888. **Profile Memory** — agent
0889. **Programming the Environment** — agent
0890. **Programming the Path** — agent
0891. **Progress Event** — fullstack
0892. **Promise** — frontend
0893. **Prompt** — agent
0894. **Prompt Injection** — agent
0895. **Props** — frontend
0896. **Protobuf** — backend / shared
0897. **Protocol** — shared
0898. **Protocol Adapter** — agent
0899. **Protocol Update** — agent
0900. **Prototype** — frontend
0901. **Prototype Chain** — frontend
0902. **Provenance** — agent
0903. **Provider** — agent
0904. **Proxy** — frontend / shared
0905. **Pub/Sub** — shared
0906. **Public Environment Variable** — fullstack
0907. **Push Notification** — agent
0908. **Python AI Service** — fullstack
0909. **Quantization** — agent
0910. **Query** — agent
0911. **Query Classification** — agent
0912. **Query Expansion** — agent
0913. **Query Plan** — backend
0914. **Query Planner** — backend
0915. **Query Processing** — agent
0916. **Query Rewrite** — agent
0917. **Queue** — backend
0918. **Quorum** — backend
0919. **Quota** — agent
0920. **Race Condition** — shared
0921. **RAG** — agent
0922. **RAG Eval** — agent
0923. **RAG Evidence** — fullstack
0924. **Range Request** — backend
0925. **Rate Limiting** — backend — alias: Rate Limit
0926. **RBAC** — agent / backend
0927. **ReAct** — agent
0928. **React Compiler** — frontend
0929. **React Key** — frontend
0930. **React Server Components** — frontend / fullstack — alias: React Server Component / Server Components
0931. **Read Committed** — backend
0932. **Read Replica** — backend
0933. **Read-after-write** — fullstack
0934. **Read-through Cache** — backend
0935. **Readable** — backend
0936. **Readback** — agent
0937. **Readiness** — backend
0938. **Readiness Gate** — fullstack
0939. **Readiness Probe** — backend
0940. **Reason** — agent
0941. **REASON Profile** — agent
0942. **Reasoning Budget** — agent
0943. **Reasoning Effort** — agent
0944. **Reasoning Model** — agent
0945. **Reasoning Routing** — agent
0946. **Recall** — agent
0947. **Recall@K** — agent
0948. **Reciprocal Rank Fusion** — agent
0949. **Reclamation** — agent
0950. **Reconciliation** — frontend
0951. **Reconnect** — fullstack
0952. **Recovery** — agent
0953. **Recovery Success Rate** — agent
0954. **Recovery-safe Point** — agent
0955. **RED Method** — backend
0956. **Reducer** — agent / frontend
0957. **Ref** — frontend
0958. **Reference-based Context** — agent
0959. **Reflect** — frontend
0960. **Reflow** — frontend
0961. **Refresh Token** — fullstack
0962. **Refresh Token Rotation** — backend / fullstack
0963. **Registry** — backend
0964. **Regression Eval** — agent
0965. **Relational Database** — backend
0966. **Release Health** — fullstack
0967. **Reliability** — shared
0968. **Render** — frontend
0969. **Render Props** — frontend
0970. **Render Tree** — frontend
0971. **Repaint** — frontend
0972. **Repeatable Read** — backend
0973. **Replan** — agent
0974. **Replay** — agent
0975. **Replica** — agent / backend
0976. **Replication** — backend
0977. **Request** — shared
0978. **Request Context** — fullstack
0979. **Request Deadline** — backend
0980. **Request ID** — backend / fullstack
0981. **Request-Response** — fullstack
0982. **requestAnimationFrame** — frontend
0983. **requestIdleCallback** — frontend
0984. **Requirement Discovery** — fullstack
0985. **Reranker** — agent
0986. **Research Evidence** — agent
0987. **Research Goal** — agent
0988. **Research Plan** — agent
0989. **Research Runtime** — agent
0990. **Residual Connection** — agent
0991. **ResizeObserver** — frontend
0992. **Resource** — shared
0993. **Resource Hint** — frontend
0994. **Resource Limit** — backend
0995. **Resource Lock** — agent
0996. **Resource Request** — backend
0997. **Resource Timing** — frontend
0998. **Resource Waterfall** — frontend
0999. **Response** — shared
1000. **Responsibility Boundary** — agent
1001. **Responsibility Distribution** — agent
1002. **Responsibility Isolation** — agent
1003. **REST** — backend
1004. **Result Reference** — agent
1005. **Resume** — agent / fullstack
1006. **Resume Token** — fullstack
1007. **Retention** — agent
1008. **Retrieval Budget** — agent
1009. **Retrieval Embedding** — agent
1010. **Retrieval Eval** — agent
1011. **Retrieval Result** — fullstack
1012. **Retriever** — agent
1013. **Retry** — backend / shared
1014. **Retry Budget** — agent / backend
1015. **Retry Storm** — backend
1016. **Retry-after** — backend
1017. **Return Contract** — agent
1018. **revalidateTag** — frontend
1019. **Revalidation** — fullstack
1020. **Reverse Proxy** — fullstack / shared
1021. **Reversibility** — agent
1022. **Reward Model** — agent
1023. **Risk-based Autonomy** — agent
1024. **RLHF** — agent
1025. **Roll-forward** — fullstack
1026. **Rollback** — agent / backend / fullstack
1027. **Rolling Update** — backend
1028. **RoPE** — agent
1029. **Route Handler** — frontend / fullstack
1030. **Route Rendering** — frontend
1031. **Router** — agent
1032. **Row** — backend
1033. **RPC** — backend / shared
1034. **RRF** — agent
1035. **RSC Payload** — frontend / fullstack
1036. **Rubric** — agent
1037. **RUM** — frontend / fullstack
1038. **Run** — agent / fullstack
1039. **Run ID** — fullstack
1040. **Runnable** — agent
1041. **Runner** — agent
1042. **Runtime** — shared
1043. **Runtime Configuration** — fullstack
1044. **Runtime Context** — agent
1045. **Runtime Data** — frontend / fullstack
1046. **Runtime Event** — agent
1047. **Runtime Integration** — frontend
1048. **Runtime State** — agent
1049. **Runtime Task** — agent / fullstack
1050. **Runtime Validation** — fullstack
1051. **Safe Point** — agent
1052. **Safety Gate** — agent
1053. **Saga** — backend
1054. **Saga Choreography** — backend
1055. **Saga Orchestration** — backend
1056. **Salt** — backend
1057. **Same-origin** — fullstack
1058. **Same-origin Policy** — frontend
1059. **SameSite** — frontend
1060. **SAML** — backend / fullstack
1061. **Sampling** — agent / shared
1062. **Sandbox** — agent
1063. **Scaling Law** — agent
1064. **Scheduler** — agent
1065. **Schema** — shared
1066. **Schema Compatibility** — agent
1067. **Schema Validation** — fullstack
1068. **Schema-first** — fullstack
1069. **SCIM** — backend / fullstack
1070. **Scope** — agent
1071. **Scope Chain** — frontend
1072. **Search Index** — agent
1073. **Secret** — backend / fullstack
1074. **Secret Management** — backend
1075. **Secret Rotation** — backend
1076. **Secure Cookie** — frontend
1077. **Security Metadata** — agent
1078. **Selection** — agent
1079. **Selection Error** — agent
1080. **Self-Attention** — agent
1081. **Self-contained Request** — agent
1082. **Self-hosted** — fullstack
1083. **Semantic Conventions** — backend / shared
1084. **Semantic Event Stream** — fullstack
1085. **Semantic HTML** — frontend
1086. **Semantic Memory** — agent
1087. **Semantic Validation** — agent
1088. **Semantic Versioning** — frontend
1089. **Semaphore** — shared
1090. **Sequential Retrieval** — agent
1091. **Sequential Scan** — backend
1092. **Sequential Tool Call** — agent
1093. **Serializable** — backend
1094. **Serialization** — shared
1095. **Serialization Boundary** — fullstack
1096. **Server** — shared
1097. **Server Abort** — fullstack
1098. **Server Action** — frontend / fullstack
1099. **Server Catalog** — agent
1100. **Server Data Fetching** — fullstack
1101. **Server Function** — frontend / fullstack
1102. **Server Secret** — fullstack
1103. **Server State** — fullstack
1104. **Server Timeout** — backend
1105. **Server-only Module** — fullstack
1106. **Serverless** — fullstack
1107. **Service** — backend
1108. **Service Account** — backend / fullstack
1109. **Service Boundary** — fullstack
1110. **Service Discovery** — backend
1111. **Service Registry** — backend
1112. **Service Worker** — frontend
1113. **Serving** — agent
1114. **Session** — agent / backend / fullstack
1115. **Session ID** — fullstack
1116. **Session Rotation** — fullstack
1117. **Session State** — fullstack
1118. **sessionStorage** — frontend
1119. **Set** — frontend
1120. **SFT** — agent
1121. **Shadow DOM** — frontend
1122. **Shadow Eval** — agent
1123. **Shadow Truth** — agent
1124. **Sharding** — backend
1125. **Shared Contract** — agent
1126. **Shared Library** — agent
1127. **Shared Responsibility** — agent
1128. **Shared Schema** — fullstack
1129. **Shared Service** — agent
1130. **Shared State** — agent
1131. **Shared Truth** — agent
1132. **Shared Type** — fullstack
1133. **Shared Worker** — frontend
1134. **Shared Workspace** — agent
1135. **SharedArrayBuffer** — backend
1136. **Short-term Memory** — agent
1137. **Side Effect** — agent
1138. **Sidecar** — backend
1139. **SIGINT** — backend
1140. **Signal-to-Noise Ratio** — agent
1141. **SIGTERM** — backend
1142. **Similarity** — agent
1143. **Site** — frontend
1144. **Skill** — agent
1145. **SLA** — agent / backend / shared
1146. **SLI** — backend / shared
1147. **Slice Evaluation** — agent
1148. **SLO** — agent / backend / shared
1149. **SLO by Journey** — fullstack
1150. **Socket** — shared
1151. **Softmax** — agent
1152. **Solution Design** — fullstack
1153. **Source Map** — frontend
1154. **Source Map Symbolication** — frontend / fullstack
1155. **Source of Truth** — agent
1156. **Source Policy** — agent
1157. **Span** — agent / backend / shared
1158. **Span ID** — shared
1159. **Sparse Activation** — agent
1160. **Sparse Retrieval** — agent
1161. **Specialist** — agent
1162. **Specificity** — frontend
1163. **Split Brain** — backend
1164. **SQL** — backend
1165. **SSE** — backend / fullstack
1166. **SSE Event** — fullstack
1167. **SSG** — frontend
1168. **SSO** — backend / fullstack
1169. **SSR** — frontend
1170. **SSR Data Flow** — fullstack
1171. **Stable Identity** — agent
1172. **Stacking Context** — frontend
1173. **Stale Data** — backend
1174. **Stale Information** — agent
1175. **Stale-while-revalidate** — fullstack
1176. **startTransition** — frontend
1177. **Startup Probe** — backend
1178. **Starvation** — shared
1179. **State** — frontend / shared
1180. **State Colocation** — frontend
1181. **State Commit** — agent
1182. **State Machine** — agent
1183. **State Machine UI** — frontend
1184. **State Owner** — agent
1185. **State Reconciliation** — agent
1186. **State Transition** — agent
1187. **State Update** — agent
1188. **State Version** — agent
1189. **StatefulSet** — backend
1190. **Stateless Protocol Core** — agent
1191. **Static Shell** — frontend / fullstack
1192. **Statistics** — backend
1193. **Status** — agent
1194. **Status Code** — backend
1195. **stdio** — agent
1196. **Sticky Session** — agent
1197. **Stop Condition** — agent
1198. **Stopping Condition** — agent
1199. **Stopping Criteria** — agent
1200. **Stream** — backend
1201. **Stream Pipeline** — backend
1202. **Streamable HTTP** — agent
1203. **Streaming Backpressure** — fullstack
1204. **Streaming Boundary** — fullstack
1205. **Streaming Rendering** — frontend
1206. **Streaming Replication** — backend
1207. **Streaming Token** — fullstack
1208. **Streaming UI** — fullstack
1209. **Strict Mode** — frontend
1210. **Strong Consistency** — backend
1211. **Structural Typing** — frontend
1212. **Structure-aware Chunking** — agent
1213. **Structured Clone** — frontend
1214. **Structured Log** — backend
1215. **Structured Logging** — shared
1216. **Structured Output** — agent
1217. **Stub** — frontend
1218. **Student Model** — agent
1219. **Subagent** — agent
1220. **Subresource Integrity** — frontend
1221. **Subtask Contract** — agent
1222. **Success Criteria** — agent
1223. **Summary** — agent
1224. **Supervision Burden** — agent
1225. **Supervisor** — agent
1226. **Suspense** — frontend
1227. **Suspense Boundary** — frontend / fullstack
1228. **SWC** — frontend
1229. **Symbol** — frontend
1230. **Sync** — shared
1231. **Sync Call** — fullstack
1232. **Synthetic Check** — fullstack
1233. **Synthetic Data** — agent
1234. **Synthetic Monitoring** — frontend
1235. **System Instruction** — agent
1236. **System Prompt** — agent
1237. **Table** — backend
1238. **Tail Latency** — agent
1239. **Tail Sampling** — backend / shared
1240. **Takeover Owner** — agent
1241. **Task** — frontend
1242. **Task Continuity** — agent
1243. **Task Eval** — agent
1244. **Task Handle** — agent
1245. **Task Identity** — agent
1246. **Task Status** — fullstack
1247. **Task World** — agent
1248. **Task-scoped** — agent
1249. **TCP** — shared
1250. **Teacher Model** — agent
1251. **Telemetry** — agent
1252. **Temperature** — agent
1253. **Tenant** — agent
1254. **Tenant Context** — fullstack
1255. **Tenant Isolation** — backend / fullstack
1256. **Tenant Knowledge** — fullstack
1257. **Tenant Resolution** — fullstack
1258. **Test-time Compute** — agent
1259. **Testing Library** — frontend
1260. **this Binding** — frontend
1261. **Thread** — agent / fullstack / shared
1262. **Thundering Herd** — backend
1263. **Time to First Token** — agent
1264. **Timeout** — agent / backend / shared
1265. **Timeout Propagation** — fullstack
1266. **TLS** — shared
1267. **TLS Handshake** — shared
1268. **TLS Termination** — backend
1269. **Token** — agent
1270. **Token Budget** — agent
1271. **Token Embedding** — agent
1272. **Token Revocation** — backend
1273. **Token Stream** — fullstack
1274. **Token-based Auth** — fullstack
1275. **Tokenization** — agent
1276. **Tokenizer** — agent
1277. **Tool** — agent
1278. **Tool Annotation** — agent
1279. **Tool Call** — fullstack
1280. **Tool Calling** — agent
1281. **Tool Contract** — agent
1282. **Tool Description** — agent
1283. **Tool Discovery** — agent
1284. **Tool Permission** — agent / fullstack
1285. **Tool Result** — agent / fullstack
1286. **Tool Result Contract** — agent
1287. **Tool Retrieval** — agent
1288. **Tool Routing** — agent
1289. **Tool Schema** — agent
1290. **Tool Selection** — agent
1291. **Tool Side Effect** — fullstack
1292. **Tool Success** — agent
1293. **Tool Surface** — agent
1294. **Tool Trace** — agent
1295. **Tool Use** — agent
1296. **Top-k** — agent
1297. **Top-K Sampling** — agent
1298. **Top-P** — agent
1299. **Topology Spread Constraint** — backend
1300. **Trace** — agent / backend / shared
1301. **Trace Context** — backend / shared
1302. **Trace ID** — shared
1303. **Trace Sampling** — backend / fullstack
1304. **traceparent** — fullstack
1305. **Training Tokens** — agent
1306. **Trajectory** — agent
1307. **Trajectory Eval** — agent
1308. **Transaction** — backend
1309. **Transaction Boundary** — fullstack
1310. **Transactional Outbox** — backend
1311. **Transferable** — backend
1312. **Transform** — backend
1313. **Transformer** — agent
1314. **Transition** — frontend
1315. **Transition Rule** — agent
1316. **Transpiler** — frontend
1317. **Transport** — agent
1318. **Tree Shaking** — frontend
1319. **Trial** — agent
1320. **Trust** — agent
1321. **Trusted Types** — frontend
1322. **Truth Commit** — agent
1323. **Truth Ownership** — agent
1324. **TTFB** — frontend
1325. **TTL** — backend
1326. **Turn** — agent
1327. **Type Checker** — agent
1328. **Type Guard** — frontend
1329. **Type Inference** — frontend
1330. **Uncaught Exception** — backend
1331. **Uncertainty** — agent
1332. **Uncontrolled Component** — frontend
1333. **Uncork** — backend
1334. **Unhandled Rejection** — backend
1335. **Union Type** — frontend
1336. **Unique Constraint** — backend
1337. **Unit Test** — frontend
1338. **UNKNOWN** — agent
1339. **Untrusted Data** — agent
1340. **Update Rule** — agent
1341. **updateTag** — frontend
1342. **Upgrade Signal** — agent
1343. **URI** — shared
1344. **URL** — shared
1345. **use cache** — frontend
1346. **USE Method** — backend
1347. **useCallback** — frontend
1348. **useDeferredValue** — frontend
1349. **useEffect** — frontend
1350. **useMemo** — frontend
1351. **useOptimistic** — frontend
1352. **User Context** — fullstack
1353. **User Journey** — fullstack
1354. **User Message** — agent
1355. **useTransition** — frontend
1356. **Utility Type** — frontend
1357. **VACUUM** — backend
1358. **Validator** — agent
1359. **Value** — agent
1360. **Variance** — frontend
1361. **Vector** — agent
1362. **Vector Clock** — backend
1363. **Vector Database** — agent
1364. **Vector Store** — agent
1365. **Version Conflict** — fullstack
1366. **Versioned Contract** — fullstack
1367. **Vertical Agent** — agent
1368. **Virtual DOM** — frontend
1369. **Visual Computer Use** — agent
1370. **Vite** — frontend
1371. **VLM** — agent
1372. **Vocabulary** — agent
1373. **Volume** — backend
1374. **VPA** — backend
1375. **WAF** — backend
1376. **WAITING** — agent / fullstack
1377. **WAL** — backend
1378. **Warm Start** — fullstack
1379. **WeakMap** — frontend
1380. **WeakSet** — frontend
1381. **Web Components** — frontend
1382. **Web Vitals** — frontend
1383. **Web Worker** — frontend
1384. **Webhook** — agent / backend
1385. **Webpack** — frontend
1386. **WebSocket** — backend / fullstack
1387. **WebSocket Frame** — fullstack
1388. **Window Function** — backend
1389. **Work Environment** — agent
1390. **Worker** — agent / backend
1391. **Worker Pool** — backend
1392. **Worker Thread** — backend
1393. **worker_threads** — backend
1394. **Workflow** — agent
1395. **Working Context** — agent
1396. **Working Memory** — agent
1397. **Working Set** — agent
1398. **Workspace** — agent / frontend
1399. **Writable** — backend
1400. **Write Permission** — agent
1401. **Write Set** — agent
1402. **Write Skew** — backend
1403. **Write-back Cache** — backend
1404. **Write-through** — backend
1405. **XSS** — frontend
1406. **z-index** — frontend
1407. **Zero-shot** — agent
