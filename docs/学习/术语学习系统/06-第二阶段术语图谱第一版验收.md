# T2｜Term Graph V1 验收

> 日期：2026-09-08
> 状态：**T2_TERM_GRAPH_V1_PASS / T3_LEARNING_RUNTIME_NEXT**

## 1. 输入

- 高召回候选唯一名：**1,415**；
- Canonical Corpus：**1,406**；
- Graph 节点源：`data/terms-v1.json`；
- 图谱文件：`data/term-graph-v1.json`。

T2 不要求把 1,406 个词全部强连；先以高价值 Hub 构建可靠主干，其余词作为深化节点逐步挂接。
## 2. V1 Graph 结果

- Hub Nodes：**411**；
- Edges：**436**；
- Weak Components：**1**；
- dangling reference：**0**；
- invalid relation type：**0**；
- self edge：**0**。

关系包括：`PREREQUISITE_OF / PART_OF / USES / PROVIDES / CONTRASTS_WITH / ALTERNATIVE_TO / IMPLEMENTED_BY / OBSERVED_BY / FAILURE_RELATED / CROSS_CHAIN`。
## 3. 路径质量抽检

- Agent：`Transformer → LLM → Agent Loop → Agent Runtime → AI Platform`；
- Frontend：`Browser → Performance API → Web Vitals`；
- Backend：`Server → Node.js → Container → Pod → Kubernetes Deployment`；
- Full-stack：`Client → Request → HTTP → Trace Context → Distributed Trace`；
- RAG：`Document → Chunking → Embedding → Dense Retrieval → RAG → RAG Eval`；
- Runtime：`Runtime Task → Checkpoint → Durable Execution → Recovery`。

裁决：`T2_TERM_GRAPH_V1 = PASS`。下一步进入 T3 Learning Runtime，开始把“查词 / 前置检查 / 最短学习路径 / 学习状态”做成可执行能力。