# T7｜Hub Content Expansion SDD

> 日期：2026-09-08
> 状态：**T7_COMPLETE / HUB_411_OF_411 / V1_COMPLETE / NO_CANONICAL_MUTATION**

## 1. 目标

T6 已证明 103 个 Interview-core Term 可以稳定提供可学习 Card。T7 不扩大 Runtime 职责，只扩展内容覆盖：

`75 / 411 Hub cards → 分 Wave 提升到 411 / 411 Hub cards`。

T7 不要求把 1,407 个 Canonical Term 全部物化；非 Hub 叶子继续由真实 JD、面试和学习反馈按需升级。

## 2. Wave 选择

Wave 1 冻结在 `data/term-card-hub-wave1-v1.json`：

- 仅从当前尚无 Card 的 Hub 中选择；
- 按 Graph degree 降序；
- 同 degree 优先跨链节点；
- 固定 80 个，不因后续图谱变化自动漂移。

第一波覆盖 Agent Loop/Harness/LLM、Retrieval/MCP、Browser/React、HTTP/Streaming、DB/K8s/Observability 等主骨架。
## 3. Authority

- Canonical name / termId：继续由 `terms-v1.json` 拥有；
- Graph relation：继续由 `term-graph-v1.json` 拥有；
- Card 内容：由 `term-card-content-seeds-v1.jsonl` 拥有，经 builder 编译；
- T6 首批 103：冻结在 `term-cards-interview-core-v1.json`，禁止因扩容改写历史验收；
- Runtime 只读取合并后的 `term-cards-v1.json`，不存在的 Card 必须返回 `null`。

## 4. Source Policy

稳定定义优先标准/官方文档；Agent 工程概念可同时引用内部 `ai-modern-evolution` 作为演进背景。框架/协议类必须有明确官方 sourceRef，不允许只靠面试题库定义。

Wave 1 新增 Docker、OpenAPI、React RSC、OWASP JWT 等官方来源；Kubernetes、MDN、Node、PostgreSQL、OpenTelemetry、MCP、OpenAI Agents SDK 继续复用现有注册表。

## 5. Gate

每个 Wave 必须满足：

1. Wave term 100% canonical 命中；
2. Wave term 100% 新增 Card；
3. T6 Interview Core 始终 103/103；
4. Card 必填字段与 sourceRef 100% 完整；
5. `interview30s` 不超 180 字，整卡关键字段不得重复；
6. `term_lookup` / progress / feedback / calibration 不回归；
7. `git diff --check` PASS。

T7 完成条件是 `411/411 Hub`，不是 `1406/1406 Canonical`。