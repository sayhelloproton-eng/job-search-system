# T5｜Continuous Calibration 验收

> 日期：2026-09-08
> 状态：**T5_PASS / SAFE_CONTINUOUS_MAINTENANCE**

## 1. 已实现

- Migration 008：Candidate / Candidate Event / Calibration Event；
- `learning.record_term_candidate`：显式候选收集，known canonical/alias 自动拒绝；
- `learning.list_term_candidates`：多源候选优先进入 curated review；
- `learning.audit_term_freshness`：按 term class 审查窗口排序；
- `learning.record_term_calibration`：记录官方/维护者核验证据；
- Candidate / Calibration event 均 append-only；
- Runtime 不直接修改静态 term / alias / graph truth。

## 2. Freshness

- Stable：365d；Evolving Standard：30d；Framework：120d；Market：30d；
- 最近 calibration event 优先作为 freshness baseline；
- 无 calibration 时以 corpus generatedAt 为 baseline。
## 3. Gate

- T5 targeted：6/6 PASS；
- T3 + T4 + T5 + Learning Bridge + Runtime CLI + DB + Context：33/33 PASS；
- `git diff --check`：PASS。

裁决：`T5_CONTINUOUS_CALIBRATION = PASS`。

## 4. 后续边界

T0–T5 完成的是：术语发现、归一、图谱、学习状态、Interview/JD 回流和持续校准。

**尚不能宣称“术语教学内容完整”。**当前除 MCP 样例外，绝大多数节点仍只有 metadata / relation，没有完整的 Definition / Background / Mechanism / Trade-off / Interview Answer。

因此下一阶段是 `T6_TERM_CARD_CONTENT_MATERIALIZATION`，先覆盖 Hub/P0，再扩叶子节点。
