# T6｜Term Card Content 第一批验收

> 日期：2026-09-08
> 状态：**T6_FIRST_BATCH_PASS / 103_INTERVIEW_CORE_CARDS**

## 1. 交付

- `data/term-card-content-seeds-v1.jsonl`：103 条人工策划内容 seed；
- `data/term-card-sources-v1.json`：机器可读 Source Registry；
- `data/term-cards-v1.json`：103 张编译后的 Compact Curated Card；
- `scripts/build-term-cards-v1.mjs`：可重复内容编译器；
- `learning.term_lookup`：返回 Card + Graph + Progress；
- `learning.get_term_card_coverage`：返回客观内容覆盖率。

## 2. Card 固定内容

每张首批 Card 至少包含：

`白话定义 → 背景 → 核心机制 → 系统位置/边界 → Failure/Trade-off → 30秒面试表达 → Source/Freshness`。

Graph 中的 prerequisite / neighbor 不重复写入 Card，由 T2 动态组合。个人学习状态继续由 SQLite T3 保存。
## 3. Quality Gate

机械内容 Gate：

- seedCount = 103；
- coreCount = 103；
- materializedCards = 103；
- uniqueTermIds = 103；
- sourceRef 100% 可解析；
- definition/background/mechanism/interview30s 无整段重复；
- `interview30s` 长度 72～180 字，平均约 121 字。

## 4. Test Gate

- T6 targeted：`7/7 PASS`；
- T3 + T6 + Context 隔离回归：`20/20 PASS`；
- T4/T5 在前序兼容 Gate 均已 PASS；
- `git diff --check = PASS`。

## 5. 裁决

```text
T6_FIRST_BATCH = PASS
INTERVIEW_CORE_CONTENT = 103 / 103
RUNTIME_CARD_LOOKUP = PASS
NON_MATERIALIZED_TERM = FAIL_CLOSED / card:null
CAREER_FACT_MUTATION = 0
NEXT = T7_HUB_CONTENT_EXPANSION
```

103 张 Card 现在是正式可学习基线；后续扩容不得反向降低这批内容质量。