# T1｜术语 Canonicalization V1 验收

> 日期：2026-09-08
> 状态：**T1_CANONICALIZATION_V1_PASS / T2_TERM_GRAPH_CURRENT**

## 1. 输入与输出

输入：`01-四链路术语范围第二版.md` 的高召回候选池。
原始挂载记录：**1,594**；大小写归一唯一名约 **1,411**。

输出：
- `data/terms-v1.json`：机器可读 canonical corpus；
- `data/aliases-v1.json`：保守 alias 映射。

当前 canonical term：**1,403**。
## 2. Canonicalization 原则

- 只自动合并确定性别名，不以字符串相似度替代语义判断；
- `State / Runtime State / Session State`、`Task / Runtime Task / MCP Task / A2A Task` 等保留独立节点；
- Framework / Market / Evolving Standard 只做分类，不反向覆盖稳定机制；
- 同一 canonical term 可以合法挂在多条 chain；
- Corpus 完整覆盖和真实学习量分开，不能把 1,403 当作待背清单。

优先级继续保留来源 band：`P0 / P0-P1 / P1 / P1-P2`；T2 再通过 Hub 与 prerequisite 抽取最短学习路径。
## 3. Gate

- canonical terms：**1,406**；
- unique ids：**1,406 / 1,406**；
- unique canonical names：**1,406 / 1,406**；
- alias collision：**0**；
- required-field missing：**0**；
- multi-chain terms：**116**；
- `git diff --check`：PASS。

裁决：`T1_CANONICALIZATION_V1 = PASS`。下一阶段只允许围绕 Graph、Hub、prerequisite 和学习路径继续，不回退到无边界扩词。