# Course Learning Runtime｜TDD 计划

> 日期：2026-09-09
> 状态：**C2_TDD_COMPLETE / RED_TO_GREEN / 8_OF_8_PASS**

## 1. RED Fixture

1. Runtime status 暴露 7 个 Course Learning 操作；
2. `course_lookup` 能按 id 读取 `course.rag-retrieval`，返回 source-backed 内容、linked terms 与 progress；
3. `plan_course_path` 能从前置 Unit 规划到 FDE/System Design；
4. 未知 Unit fail-closed：`LEARNING_COURSE_NOT_FOUND`；
5. Progress 同 requestId 重试幂等，不同 payload 冲突；
6. Progress Event append-only；
7. due review 只返回到期课程；
8. Course Progress 不修改 Term Progress、Interview Evidence、Career Facts；
9. coverage 固定返回 7/7 materialized，并暴露 unresolved term candidates；
10. Host `/health` 继续保持 Interview ready，不因课程层改变原 42/82 Gate。

## 2. 最小实现边界

- 新增 `009_learning_course_v1.sql`，只建 Course Progress snapshot + append-only events；
- 新增 `learning-course-runtime.ts`；
- 只在现有 `interview-runtime-cli.ts` 注册 Course Learning contracts；
- 不修改 Interview Director、Question Corpus、Career Facts、Resume Evidence；
- 不把 Course Unit 正文写入 SQLite。

## 3. Stage Gate

先跑 Course targeted tests，再跑 Terminology + Learning Bridge + Host blast-radius；生产代码稳定后才更新 CURRENT。
