# Course Learning System｜旧课程内容运行时迁移实现与验收

> 日期：2026-09-09
> 状态：**PASS / 7_OF_7_COURSE_UNITS / COURSE_RUNTIME_8_OF_8 / FULL_TEST_290_OF_290**

## 1. 本轮目标

把 `docs/学习/内容/01～07` 从静态 Markdown 课程资产升级成正式 Learning Runtime 内容，同时保留原文作为 Source，不建立第二套事实真源。

固定分层：

`Term Card → 单概念理解`
`Course Unit → 多概念组成可解释、可实践、可排错、可设计的能力`
`Interview Corpus → 验证能力`
`Career Facts → 真实经历真源`

## 2. 内容迁移结果

首批 7/7 Course Unit 已物化：
- Agent Runtime / Tool / MCP / Permission；
- RAG / Retrieval / Vector DB；
- Python / FastAPI 第二服务端路径；
- Backend Production Completeness / System Design；
- Context / State / Memory / Orchestration；
- Testing / Eval / CI / Docker / Reliability；
- Framework / FDE / System Design。

结构化 Truth：`data/course-units-v1.json`；编译器：`scripts/build-course-units-v1.mjs`。
## 3. Term Bridge 与缺词审计

7 个 Course Unit 共链接 **87 个现有 Canonical Terms**；每个 linked term 都能解析到 `terms-v1.json`，Course Unit 不复制术语定义。

课程迁移反向发现 9 个尚未进入 Canonical Corpus 的概念：

`ACL / FastAPI / Pydantic / Dependency Injection / HTTPX / Lifespan / pytest / Groundedness / FDE`

这些概念全部保持 `TERM_CANDIDATE_REVIEW_REQUIRED`，当前 **不计入 1407 Canonical Terms**，不得绕过 T5 Calibration 直接造 termId。

## 4. Runtime 交付

新增 `learning-course-runtime.ts`，正式暴露 7 个 Course Learning 操作：
- `learning.list_course_units`
- `learning.course_lookup`
- `learning.plan_course_path`
- `learning.record_course_progress`
- `learning.get_course_progress`
- `learning.list_due_course_reviews`
- `learning.get_course_coverage`

新增 Migration 009，仅保存 Course Progress snapshot 与 append-only events；课程正文仍由结构化 JSON 拥有。
## 5. Test / Regression Gate

TDD 首次 RED：`learning-course-runtime.ts` 不存在，能力缺失按预期失败。

最终 Gate：
- Course Runtime targeted：**8/8 PASS**；
- Course + Terminology + Learning Feedback + Learning Bridge + Host blast-radius：**27/27 PASS**；
- Course Unit 连续两次编译 SHA 一致：**DETERMINISTIC_BUILD=PASS**；
- 4318 Host 重载后：`ready=true / 42 Formal / 82 Active / 7 Course Operations / 7 Course Units / 87 linked terms`；
- 全仓：**290/290 PASS / 0 fail**；
- `git diff --check` 在提交前必须再次 PASS。

## 6. 边界验收

- 原 `内容/01～07` 保留为 Source，没有被课程 Runtime 覆盖；
- `面试题库.md / 高匹配岗位专项面试包.md` 没有复制成 Course Unit；
- `30秒与3分钟回答库.md` 仍属于后续 Speaking/Transfer 层；
- Course Progress 与 Term Progress 独立；
- Course 学习不会自动修改 Interview Evidence、Career Facts 或 Resume Evidence；
- P6-7 真实 Voice Pilot 仍是人工参与门禁，本轮课程升级不伪造 Voice PASS。

## 7. 结论

`COURSE_LEARNING_SYSTEM_V1 = PASS / 7_OF_7 / 87_LINKED_TERMS / 9_TERM_CANDIDATES / MIGRATION_009 / RUNTIME_8_OF_8 / FULL_290_OF_290`。

旧课程内容已经从“静态 Markdown 资产”升级为正式 Learning Runtime 第二类内容；下一步如扩课，必须继续复用 Course Unit → Canonical Term → Progress/Review → Interview Bridge，而不是重新制造孤立课程目录。